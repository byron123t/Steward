from setuptools import find_packages
from setuptools import setup

setup(name='steward',
          version='1.0',
          license='MIT',
          install_requires=[
          ],
          packages=find_packages())
